import React from 'react';
import ReactDOM from 'react-dom/client';
import ClickableGrid from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ClickableGrid />
  </React.StrictMode>
);
